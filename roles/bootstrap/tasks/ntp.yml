---

- name: install ntpd
  apt:
    name: "{{ item }}"
    state: present
  with_items:
   - ntp
  notify: restart ntp


- name: open firewalld for ntp
  command: firewall-cmd --permanent --add-service=ntp
  notify: reload firewalld
