---

dependencies:
  - role: server
    vars:
      apt_sources:
        - bullseye
        - bullseye-backports # for pcengines_apu3b4
  - pcengines_apu3b4
  - cockpit
  - git_server

# TODO remove in ansible >= 2.10 when ansible-lint is not angry
galaxy_info:
  author: viccuad
  description: local
  license: "GPL3+"
  min_ansible_version: 2.9
  platforms:
  - name: Debian
    versions:
    - all
