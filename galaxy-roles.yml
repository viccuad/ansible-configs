# install with:
# ansible-galaxy install -r galaxy-roles.yml --roles-path ./roles --force

- src: https://github.com/jacoelho/ansible-softwareraid
  name: softwareraid
  version: v1.2.0

- src: git@iris:private/ansible-private.git
  name: private
  version: master
